# JavaAddressRangeTree
Java - IP Address Range Tree with Search

It's setup in a Main for testing, but you can extract the AddressTree class for use.

It builds the balanced binary tree from a `String[][]` because it assume it will come from a CSV with three fields.
